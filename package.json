{
  "name": "mini-project",
  "version": "1.0.0",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "start": "npx http-server -p 3000",
    "test": "npx playwright test"
  }
}
